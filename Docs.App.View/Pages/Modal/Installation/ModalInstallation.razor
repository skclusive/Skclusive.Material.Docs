@namespace Skclusive.Material.Docs.App.View

<MarkdownSegment
    Text="@(@"
# Modal Installation

You can [install Modal package](https://www.nuget.org/packages/Skclusive.Material.Modal/) by typing the below command in your terminal:

```csharp
dotnet add package Skclusive.Material.Modal
```

Then, you can import it in your `_Imports.razor`.

```csharp
@using Skclusive.Material.Modal
```

Then, you can render the style for this component it in your `Router.razor`.

```html
<ModalStyles />
```

")" />
