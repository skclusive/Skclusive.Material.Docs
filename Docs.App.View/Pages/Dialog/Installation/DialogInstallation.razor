@namespace Skclusive.Material.Docs.App.View

<MarkdownSegment
    Text="@(@"
# Dialog Installation

if you want to use Dialog alone follow the standalone installation. otherwise to use all components, [compound installation](./installation) can be followed.

You can [install Dialog package](https://www.nuget.org/packages/Skclusive.Material.Dialog/) by typing the below command in your terminal:

```csharp
dotnet add package Skclusive.Material.Dialog
```

Then, you can import it in your `_Imports.razor`.

```csharp
@using Skclusive.Material.Dialog
```

Then in your `App.razor`, add `ThemeProvider` wrapping Router.

```html
<ThemeProvider>
    <Router AppAssembly=""@typeof(Program).Assembly"">
        ...
    </Router>
</ThemeProvider>
```

Then in your `services (ServiceCollection)` add below service.

```csharp
services.TryAddDialogServices(new MaterialConfigBuilder().Build());
```

")" />
