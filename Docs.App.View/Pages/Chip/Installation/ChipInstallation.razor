@namespace Skclusive.Material.Docs.App.View

<MarkdownSegment
    Text="@(@"
# Chip Installation

You can [install Chip package](https://www.nuget.org/packages/Skclusive.Material.Chip/) by typing the below command in your terminal:

```csharp
dotnet add package Skclusive.Material.Chip
```

Then, you can import it in your `_Imports.razor`.

```csharp
@using Skclusive.Material.Chip
```

Then, you can render the style for this component it in your `Router.razor`.

```html
<ChipStyle />
```

")" />
