@namespace Skclusive.Material.Docs
@inherits StyleComponentBase

<style data-name="Section">
    .Section-Root {
        @* color: #fff; *@
        font-size: 16px;
        word-break: break-word;
        font-family: "Roboto", "Helvetica", "Arial", sans-serif;
    }

    .Section-Root pre {
        margin: 24px 0;
        padding: 12px 18px;
        overflow: auto;
        direction: ltr;
        border-radius: 4px;
        @* background-color: #272c34; *@
        -webkit-overflow-scrolling: touch;
    }

    .Section-Root code {
        @* color: #fff; *@
        display: inline-block;
        padding: 2px 6px;
        font-size: 14px;
        font-family: Consolas, "Liberation Mono", Menlo, Courier, monospace;
        border-radius: 2px;
        @* background-color: rgba(255, 229, 100, 0.2); *@
        -webkit-font-smoothing: subpixel-antialiased;
    }

    .Section-Root code[class*="language-"] {
        @* color: #fff; *@
        @* background-color: #272c34; *@
    }

    .Section-Root p code,
    .Section-Root ul code,
    .Section-Root pre code {
        font-size: 14px;
    }

    .Section-Root .token.operator {
        background: transparent;
    }

    .Section-Root .description {
        margin: 0 0 40px;
        font-size: 1.5rem;
        font-family: "Roboto", "Helvetica", "Arial", sans-serif;
        font-weight: 400;
        line-height: 1.334;
        letter-spacing: 0em;
        margin-bottom: 20px;
    }

    .Section-Root p,
    .Section-Root ul,
    .Section-Root ol {
        margin-top: 0;
        line-height: 1.6;
        margin-bottom: 16px;
    }

    .Section-Root ul {
        padding-left: 30px;
    }

    .Section-Root table {
        width: 100%;
        display: block;
        overflow: hidden;
        overflow-x: auto;
        word-break: normal;
        margin-bottom: 16px;
        border-spacing: 0;
        border-collapse: collapse;
        -webkit-overflow-scrolling: touch;
    }

    .Section-Root td {
        @* color: #fff; *@
        padding: 16px;
        font-size: 0.875rem;
        font-family: "Roboto", "Helvetica", "Arial", sans-serif;
        font-weight: 400;
        line-height: 1.43;
        border-bottom: 1px solid rgba(255, 255, 255, 0.12);
        letter-spacing: 0.01071em;
    }

    .Section-Root td code {
        font-size: 13px;
        line-height: 1.6;
    }

    .Section-Root th {
        @* color: #fff; *@
        padding: 16px;
        font-size: 14px;
        font-weight: 500;
        line-height: 1.5rem;
        white-space: pre;
        border-bottom: 1px solid rgba(255, 255, 255, 0.12);
    }

    .Section-Root blockquote {
        margin: 24px 0;
        padding: 4px 24px;
        border-left: 5px solid #ffe564;
        background-color: rgba(255, 229, 100, 0.2);
    }

    .Section-Root a,
    .Section-Root a code {
        @* color: #f48fb1; *@
        text-decoration: none;
    }

    .Section-Root img {
        max-width: 100%;
    }

    .Section-Root hr {
        border: none;
        height: 1px;
        margin: 48px 0px;
        flex-shrink: 0;
        background-color: rgba(255, 255, 255, 0.12);
    }

    .Section-Root a:hover,
    .Section-Root a code:hover {
        text-decoration: underline;
    }

    .Section-Root blockquote p {
        margin-top: 16px;
    }

    .Section-Root table .prop-name {
        font-size: 13px;
        font-family: Consolas, "Liberation Mono", Menlo, monospace;
    }

    .Section-Root table .required {
        @* color: #a5ffa5; *@
    }

    .Section-Root table .prop-type {
        @* color: #ffb6ec; *@
        font-size: 13px;
        font-family: Consolas, "Liberation Mono", Menlo, monospace;
    }

    .Section-Root table .prop-default {
        font-size: 13px;
        font-family: Consolas, "Liberation Mono", Menlo, monospace;
        border-bottom: 1px dotted rgba(255, 255, 255, 0.5);
    }

    .Section-Root h1 code,
    .Section-Root h2 code,
    .Section-Root h3 code,
    .Section-Root h4 code {
        font-size: inherit;
        word-break: break-all;
        line-height: inherit;
    }

</style>